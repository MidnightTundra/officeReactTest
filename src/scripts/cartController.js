//import {ObjectID} from MongoDB;

async function handleAddToCart (props, e) {
    /*
    e.preventDefault();

    const newCartItem = db.findOne(ObjectID(props.itemID))

    await fetch("user:id/cart/add", {
        method : "POST",
        headers : {
            "Content-Type" : "application/JSON"
        },
        body : JSON.stringify(newCartItem)
    });
    */
}